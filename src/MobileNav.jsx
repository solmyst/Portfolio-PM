// Mobile navigation is now integrated into the main Navigation component
// This file is no longer needed but kept for compatibility

const MobileNav = () => {
  return null;
};

export default MobileNav;